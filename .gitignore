# dependencies
node_modules/
server/node_modules/

# environment variables (contain real credentials — never commit)
.env
.env.local
.env.*.local
server/.env
server/.env.local

# build output
dist/
server/dist/

# locally-stored uploads (fallback storage when Supabase Storage bucket isn't set up)
server/data/uploads/

# logs
*.log
npm-debug.log*

# editor/OS
.DS_Store
Thumbs.db

# local tooling config
.claude/
